import { StyleSheet } from 'react-native';

import colors from '../../theme/colors';

export default StyleSheet.create({
  buttonWrapper: {
    flex: 1,
    width: 300,
  },
  button: {
    width: 300,
  },
});
